"""
BXL graph structure test: Cloud Hypervisor target validation.

Run:
    buck2 bxl buckos//tests/graph/test_cloud_hypervisor.bxl:test_cloud_hypervisor
"""

load("//tests/graph:test_cloud_hypervisor_impl.bzl", "run")

def _impl(ctx):
    passed, failed = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed".format(failed))

test_cloud_hypervisor = bxl_main(
    impl = _impl,
    cli_args = {},
)
