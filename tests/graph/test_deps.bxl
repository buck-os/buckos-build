"""
BXL graph structure test: dependency edges.

Run:
    buck2 bxl buckos//tests/graph/test_deps.bxl:test_deps
"""

load("//tests/graph:test_deps_impl.bzl", "run")

def _impl(ctx):
    passed, failed = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed".format(failed))

test_deps = bxl_main(
    doc = "Verify dependency edges in migrated packages.",
    impl = _impl,
    cli_args = {},
)
