"""
BXL graph structure test: version data audit.

Run:
    buck2 bxl buckos//tests/graph/test_versions.bxl:test_versions
"""

load("//tests/graph:test_versions_impl.bzl", "run")

def _impl(ctx):
    passed, failed = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed".format(failed))

test_versions = bxl_main(
    doc = "Audit version data via http_file archive target attributes.",
    impl = _impl,
    cli_args = {},
)
