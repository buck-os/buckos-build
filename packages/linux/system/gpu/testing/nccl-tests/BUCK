load("//defs:package_defs.bzl", "autotools_package")

autotools_package(
    name = "nccl-tests",
    version = "2.28",
    src_uri = "https://github.com/NVIDIA/nccl-tests/archive/refs/tags/v2.28.tar.gz",
    sha256 = "TODO",
    configure_args = [],
    make_args = [
        "PREFIX=${INSTALL_DIR}",
        "NCCL_HOME=${INSTALL_DIR}",
        "CUDA_HOME=/usr/local/cuda",
    ],
    deps = [
        "//packages/linux/system/gpu/libraries/nccl",
    ],
)
