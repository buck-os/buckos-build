load("//defs:package.bzl", "package")

package(
    build_rule = "binary",
    name = "checksec",
    version = "2.6.0",
    install_script = """
        mkdir -p "$OUT/usr/bin"
        install -m 0755 "$SRCS/checksec" "$OUT/usr/bin/checksec"
    """,
    visibility = ["PUBLIC"],
)
